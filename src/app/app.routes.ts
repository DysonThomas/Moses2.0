import { Routes } from '@angular/router';
import { Profile } from '../Components/profile/profile';
import { App } from './app';

export const routes: Routes = [{ path: 'profile', component: Profile }];
